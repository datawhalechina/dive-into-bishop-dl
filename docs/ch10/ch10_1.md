# 1 计算机视觉

CNN 之所以推动了计算机视觉的发展，关键在于其相较于传统全连接网络具有 **稀疏连接、参数共享、不变性和等变性** 的优势：

- 在全连接网络中，相邻层的神经元完全连接，导致参数规模庞大。  
- 在 CNN 中，同一层共享卷积核进行局部感受野计算，实现了稀疏连接与参数共享。  
- 卷积核通过滑动窗口捕捉相邻像素的局部相关性，不仅显著减少参数量，还使模型更聚焦于局部结构特征。  
- CNN 在处理图像时，即使图像发生位置或形状的变化，依然能够识别目标或保持特征间的关联性，这正体现了其对平移等变性与特征不变性的有效建模能力。  

